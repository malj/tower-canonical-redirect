[package]
name = "tower-canonical-redirect"
version = "0.1.0"
rust-version = "1.85"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/malj/tower-canonical-redirect"
description = "A Tower middleware to enforce canonical hosts in HTTP requests"
keywords = ["tower", "middleware", "canonical", "redirect", "axum"]
categories = [
  "asynchronous",
  "network-programming",
  "web-programming::http-server",
]

[dependencies]
futures = "0.3.31"
http = "1.4.0"
tower = "0.5.3"

[dev-dependencies]
test-case = "3.3.1"
